---
const { title, description, href } = Astro.props;
---

<a href={href} class="block bg-black-800 border-black-700 border-2 rounded-lg p-4">
    <h1 class="text-xl font-bold">{title}</h1>
    <p>{description}</p>
</a>
